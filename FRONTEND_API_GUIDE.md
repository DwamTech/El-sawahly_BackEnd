


---

## 1. المقالات (Articles)
**الرابط الأساسي:** `/api/articles`

### إضافة مقال جديد (POST)
| الحقل | النوع | الحالة | ملاحظات |
|-------|-------|--------|---------|
| `title` | نص (String) | **مطلوب** | عنوان المقال. |
| `slug` | نص (String) | **مطلوب** | الرابط اللطيف، يجب أن يكون فريداً. |
| `content` | نص (String/HTML) | **مطلوب** | محتوى المقال. |
| `author_name` | نص (String) | اختياري | اسم الكاتب. |
| `status` | نص (String) | **مطلوب** | القيم المسموحة: `draft`, `published`, `archived`. |
| `section_id` | رقم (Integer) | اختياري | معرف القسم التابع له المقال. |
| `excerpt` | نص (String) | اختياري | مقتطف قصير عن المقال. |
| `featured_image` | ملف (Image) | اختياري | الصورة البارزة (jpeg, png, jpg, gif, webp)، الحد الأقصى 1MB. |
| `gregorian_date` | نص (String) | اختياري | التاريخ الميلادي. |
| `hijri_date` | نص (String) | اختياري | التاريخ الهجري. |
| `references` | نص (String) | اختياري | المراجع. |
| `keywords` | نص (String) | اختياري | الكلمات الدلالية. |
| `published_at` | تاريخ (Date) | اختياري | تاريخ النشر. |

### تعديل مقال (POST/PUT)
*نفس حقول الإضافة، ولكن جميعها اختيارية (Sometimes) عند التعديل، ما عدا القواعد التي تعتمد على بعضها.*
*عند استخدام `PUT`، يجب إرسال البيانات بصيغة `x-www-form-urlencoded` أو استخدام `POST` مع حقل `_method=PUT` لرفع الملفات.*

### حذف مقال (DELETE)
* **الرابط:** `/api/articles/{id}`

---

## 2. الصوتيات (Audios)
**الرابط الأساسي:** `/api/audios`

### إضافة صوتي جديد (POST)
| الحقل | النوع | الحالة | ملاحظات |
|-------|-------|--------|---------|
| `title` | نص (String) | **مطلوب** | عنوان المادة الصوتية. |
| `type` | نص (String) | **مطلوب** | القيم المسموحة: `upload` (رفع ملف), `link` (رابط خارجي). |
| `file` | ملف (File) | **مشروط** | مطلوب إذا كان `type` = `upload`. |
| `url` | رابط (URL) | **مشروط** | مطلوب إذا كان `type` = `link`. |
| `section_id` | رقم (Integer) | اختياري | معرف القسم. |
| `description` | نص (String) | اختياري | وصف المادة. |
| `thumbnail` | ملف (Image) | اختياري | صورة مصغرة. |
| `keywords` | نص (String) | اختياري | |
| `rating` | رقم (Number) | اختياري | من 0 إلى 5. |

### تعديل صوتي (POST/PUT)
*نفس الحقول، جميعها اختيارية.*

---

## 3. المرئيات (Visuals)
**الرابط الأساسي:** `/api/visuals`

### إضافة مرئي جديد (POST)
| الحقل | النوع | الحالة | ملاحظات |
|-------|-------|--------|---------|
| `title` | نص (String) | **مطلوب** | عنوان المادة المرئية. |
| `type` | نص (String) | **مطلوب** | القيم المسموحة: `upload`, `link`. |
| `file` | ملف (File) | **مشروط** | مطلوب إذا كان `type` = `upload`. |
| `url` | رابط (URL) | **مشروط** | مطلوب إذا كان `type` = `link`. |
| `section_id` | رقم (Integer) | اختياري | معرف القسم. |
| `description` | نص (String) | اختياري | |
| `thumbnail` | ملف (Image) | اختياري | صورة مصغرة. |
| `keywords` | نص (String) | اختياري | |
| `rating` | رقم (Number) | اختياري | من 0 إلى 5. |

---

## 4. المعرض (Galleries)
**الرابط الأساسي:** `/api/galleries`

### إضافة معرض جديد (POST)
| الحقل | النوع | الحالة | ملاحظات |
|-------|-------|--------|---------|
| `name` | نص (String) | **مطلوب** | اسم المعرض. |
| `section_id` | رقم (Integer) | اختياري | معرف القسم. |
| `description` | نص (String) | اختياري | وصف المعرض. |
| `image` | ملف (Image) | اختياري | الصورة الرئيسية للمعرض. |
| `images[]` | مصفوفة ملفات | اختياري | صور إضافية داخل المعرض (يمكن رفع ملفات متعددة). |
| `keywords` | نص (String) | اختياري | |

### تعديل معرض (POST/PUT)
*بالإضافة للحقول أعلاه، يمكن استخدام:*
| الحقل | النوع | ملاحظات |
|-------|-------|---------|
| `delete_image_ids[]` | مصفوفة أرقام | معرفات الصور الفرعية المراد حذفها من المعرض. |

---

## 5. الكتب (Books)
**الرابط الأساسي:** `/api/books` (Admin: `/api/admin/books` - يرجى التأكد من الروت الفعلي في `routes/api.php`)

### إضافة كتاب جديد (POST)
| الحقل | النوع | الحالة | ملاحظات |
|-------|-------|--------|---------|
| `title` | نص (String) | **مطلوب** | عنوان الكتاب. |
| `description` | نص (String) | **مطلوب** | وصف الكتاب. |
| `source_type` | نص (String) | **مطلوب** | `file` (ملف), `link` (رابط), `embed` (تضمين). |
| `file_path` | ملف (File) | **مشروط** | مطلوب إذا كان `source_type` = `file`. (PDF, DOC, EPUB). |
| `source_link` | نص (String) | **مشروط** | مطلوب إذا كان `source_type` = `link` أو `embed`. |
| `cover_type` | نص (String) | **مطلوب** | `auto` (تلقائي), `upload` (رفع). |
| `cover_path` | ملف (Image) | **مشروط** | مطلوب إذا كان `cover_type` = `upload`. |
| `author_name` | نص (String) | **مطلوب** | اسم المؤلف. |
| `type` | نص (String) | **مطلوب** | `single` (كتاب مفرد), `part` (جزء من سلسلة). |
| `book_series_id` | رقم (Integer) | **مشروط** | مطلوب إذا كان `type` = `part`. |
| `keywords` | مصفوفة/نص | اختياري | |

---

## 6. سلاسل الكتب (Book Series)
**الرابط الأساسي:** `/api/book-series`

### إضافة سلسلة (POST)
| الحقل | النوع | الحالة | ملاحظات |
|-------|-------|--------|---------|
| `name` | نص (String) | **مطلوب** | اسم السلسلة. |
| `description` | نص (String) | اختياري | وصف السلسلة. |

---

## 7. الوثائق والملفات (Documents)
**الرابط الأساسي:** `/api/documents`

### إضافة وثيقة (POST)
| الحقل | النوع | الحالة | ملاحظات |
|-------|-------|--------|---------|
| `title` | نص (String) | **مطلوب** | عنوان الوثيقة. |
| `source_type` | نص (String) | **مطلوب** | `file`, `link`. |
| `file_path` | ملف (File) | **مشروط** | مطلوب إذا كان `source_type` = `file`. يدعم (pdf, doc, xls, ppt, txt, zip...). |
| `source_link` | رابط (URL) | **مشروط** | مطلوب إذا كان `source_type` = `link`. |
| `cover_type` | نص (String) | **مطلوب** | `auto`, `upload`. |
| `cover_path` | ملف (Image) | **مشروط** | مطلوب إذا كان `cover_type` = `upload`. |
| `description` | نص (String) | اختياري | |
| `keywords` | مصفوفة/نص | اختياري | |

---

## 8. الأعداد (Issues)
**الرابط الأساسي:** `/api/issues`

### إضافة عدد جديد (POST)
| الحقل | النوع | الحالة | ملاحظات |
|-------|-------|--------|---------|
| `title` | نص (String) | **مطلوب** | عنوان العدد. |
| `slug` | نص (String) | **مطلوب** | رابط فريد. |
| `status` | نص (String) | **مطلوب** | `draft`, `published`, `archived`. |
| `issue_number` | رقم (Integer) | اختياري | رقم العدد (يجب أن يكون فريداً). |
| `cover_image` | ملف (Image) | اختياري | صورة الغلاف. |
| `pdf_file` | ملف (PDF) | اختياري | ملف العدد كاملاً (PDF). |
| `hijri_date` | نص (String) | اختياري | |
| `gregorian_date` | نص (String) | اختياري | |
| `is_featured` | منطقي (Boolean)| اختياري | `1` أو `0` (مميز). |
| `sort_order` | رقم (Integer) | اختياري | ترتيب العرض. |

---

## 9. الروابط (Links)
**الرابط الأساسي:** `/api/links`

### إضافة رابط (POST)
| الحقل | النوع | الحالة | ملاحظات |
|-------|-------|--------|---------|
| `title` | نص (String) | **مطلوب** | عنوان الرابط. |
| `url` | رابط (URL) | **مطلوب** | الرابط الفعلي. |
| `section_id` | رقم (Integer) | اختياري | |
| `description` | نص (String) | اختياري | |
| `image` | ملف (Image) | اختياري | صورة معبرة عن الرابط. |
| `rating` | رقم (Number) | اختياري | |

---

## 10. الأقسام (Sections)
**الرابط الأساسي:** `/api/sections`

### إضافة قسم (POST)
| الحقل | النوع | الحالة | ملاحظات |
|-------|-------|--------|---------|
| `name` | نص (String) | **مطلوب** | اسم القسم (فريد). |
| `description` | نص (String) | اختياري | |
| `is_active` | منطقي (Boolean)| اختياري | `1` (نشط) أو `0` (غير نشط). الافتراضي `1`. |

---

## 11. المستخدمين (Users)
**الرابط الأساسي:** `/api/users` (للمدير)

### إضافة مستخدم (POST)
| الحقل | النوع | الحالة | ملاحظات |
|-------|-------|--------|---------|
| `name` | نص (String) | **مطلوب** | الاسم. |
| `email` | بريد (Email) | **مطلوب** | بريد إلكتروني فريد. |
| `password` | نص (String) | **مطلوب** | كلمة المرور (على الأقل 8 أحرف). |
| `role` | نص (String) | اختياري | الدور: `admin`, `editor`, `author`, `reviewer`, `user`. |

### تعديل الملف الشخصي (للمستخدم الحالي) - `/api/profile/update`
| الحقل | النوع | الحالة | ملاحظات |
|-------|-------|--------|---------|
| `name` | نص (String) | اختياري | |
| `email` | بريد (Email) | اختياري | |
| `current_password` | نص (String) | **مشروط** | مطلوبة فقط إذا كنت تريد تغيير كلمة المرور. |
| `new_password` | نص (String) | اختياري | كلمة المرور الجديدة (تتطلب `current_password`). |

---

## 12. طلبات الدعم الفردي (Individual Support)
**الرابط:** `/api/support/individual`

### تقديم طلب (POST)
| الحقل | النوع | الحالة | ملاحظات |
|-------|-------|--------|---------|
| `full_name` | نص | **مطلوب** | الاسم الرباعي. |
| `gender` | نص | **مطلوب** | `male`, `female`. |
| `nationality` | نص | **مطلوب** | الجنسية. |
| `city` | نص | **مطلوب** | المدينة. |
| `phone_number` | نص | **مطلوب** | رقم الجوال. |
| `whatsapp_number` | نص | **مطلوب** | رقم الواتساب. |
| `email` | بريد | **مطلوب** | |
| `birth_date` | تاريخ | **مطلوب** | تاريخ الميلاد. |
| `identity_image_path` | ملف | **مطلوب** | صورة الهوية. |
| `identity_expiry_date` | تاريخ | **مطلوب** | تاريخ انتهاء الهوية. |
| `housing_type` | نص | **مطلوب** | نوع السكن. |
| `academic_qualification_path` | ملف | **مطلوب** | المؤهل العلمي. |
| `scientific_activity` | نص | **مطلوب** | النشاط العلمي. |
| `cv_path` | ملف | **مطلوب** | السيرة الذاتية (PDF/DOC). |
| `workplace` | نص | **مطلوب** | جهة العمل. |
| `support_scope` | نص | **مطلوب** | `full` (كلي), `partial` (جزئي). |
| `amount_requested` | رقم | **مطلوب** | المبلغ المطلوب. |
| `support_type` | نص | **مطلوب** | نوع الدعم. |
| `has_income` | منطقي | **مطلوب** | هل يوجد دخل ثابت؟ (`1`/`0`). |
| `marital_status` | نص | **مطلوب** | `single`, `married`. |
| `bank_account_iban` | نص | **مطلوب** | رقم الآيبان. |
| `bank_name` | نص | **مطلوب** | اسم البنك. |

---

## 13. طلبات دعم المؤسسات (Institutional Support)
**الرابط:** `/api/support/institutional`

### تقديم طلب (POST)
| الحقل | النوع | الحالة | ملاحظات |
|-------|-------|--------|---------|
| `institution_name` | نص | **مطلوب** | اسم الجهة. |
| `license_number` | نص | **مطلوب** | رقم الترخيص. |
| `license_certificate_path` | ملف | **مطلوب** | صورة الترخيص. |
| `email` | بريد | **مطلوب** | البريد الرسمي. |
| `support_letter_path` | ملف | **مطلوب** | خطاب طلب الدعم. |
| `ceo_name` | نص | **مطلوب** | اسم المدير التنفيذي. |
| `project_name` | نص | **مطلوب** | اسم المشروع. |
| `project_file_path` | ملف | **مطلوب** | ملف المشروع. |
| `operational_plan_path` | ملف | **مطلوب** | الخطة التشغيلية. |
| `bank_certificate_path` | ملف | **مطلوب** | شهادة الحساب البنكي. |
| *وحقول أخرى كثيرة (راجع الكنترولر للتفاصيل الدقيقة)* | | | |

---

## 14. التقييم والشكاوى (Feedback & Rating)

### إرسال شكوى/اقتراح (POST `/api/feedback`)
| الحقل | النوع | الحالة | ملاحظات |
|-------|-------|--------|---------|
| `name` | نص | **مطلوب** | الاسم. |
| `email` | بريد | **مطلوب** | |
| `message` | نص | **مطلوب** | نص الرسالة. |
| `type` | نص | **مطلوب** | `suggestion` (اقتراح), `complaint` (شكوى). |
| `attachment_path` | ملف | اختياري | مرفق. |

### تقييم المنصة (POST `/api/platform-rating`)
| الحقل | النوع | الحالة | ملاحظات |
|-------|-------|--------|---------|
| `rating` | رقم (Integer) | **مطلوب** | من 1 إلى 5. |

---

## 15. تواصل معنا (Site Contact)
**الرابط:** `/api/admin/site-contact` (للتحديث)

### تحديث البيانات (PUT)
| الحقل | النوع | الحالة | ملاحظات |
|-------|-------|--------|---------|
| `phone` | نص | اختياري | رقم الهاتف الرئيسي. |
| `email` | بريد | اختياري | البريد الإلكتروني. |
| `twitter`, `facebook`, `youtube`... | روابط | اختياري | روابط التواصل الاجتماعي. |
